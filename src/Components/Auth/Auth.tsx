import React, {useEffect} from "react";

function Auth() {
    useEffect(() => {
        return () => {
            localStorage.removeItem("")
        }
    })
    return (
        <>
        </>
    )
}

export default Auth;